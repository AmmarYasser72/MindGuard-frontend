# MindGuard React



## Scripts

- `npm install`
- `npm run dev`
- `npm run build`

The app uses the existing SVG assets from `public/assets/icons` and keeps the current routes, demo auth flows, patient wellness tools, analytics tabs, chat persistence, and doctor dashboard flows.
